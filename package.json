{
  "name": "dynamic-fluid-caustics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interactive wave-control & inverse-rendering caustic simulator (WebGPU).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "@webgpu/types": "^0.1.40",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
